<form method="post" action="{{ path('skill_delete', {'id': skill.id}) }}" onsubmit="Êtes-Vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
