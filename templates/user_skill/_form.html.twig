{{ form_start(form) }}
        {{ form_row(form.user_id, {
                label: 'Nom de la personne'
        }) }}
        {{ form_row(form.skill_id, {
                label: 'Nomde la compétence'
        }) }}
        {{ form_row(form.level, {
                label: 'Niveau de la compétence'
        }) }}
        {{ form_row(form.liked, {
                label: 'Niveau d\'appréciation de la compétence'
        }) }}
        {{ form_row(form.register, {
                label: 'Modifier'
        }) }}
{{ form_end(form) }}
