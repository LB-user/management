{{ form_start(form) }}
        {{ form_row(form.user_id, {
                label: 'Nom'
        }) }}
        {{ form_row(form.skill_id, {
                label: 'Prénom'
        }) }}
        {{ form_row(form.level, {
                label: 'Adresse'
        }) }}
        {{ form_row(form.liked, {
                label: 'Numéro de téléphone'
        }) }}
        {{ form_row(form.register, {
                label: 'Modifier'
        }) }}
{{ form_end(form) }}
