{% extends 'base.html.twig' %}

{% block body %}

	<div class="container">
		<h1>Compétence</h1>
		<table class="table">
			<tbody>
				<tr>
					<th>Nom de la Compétence</th>
					<td>{{ skill.name }}</td>
				</tr>
			</tbody>
		</table>
		<a class="btn btn-primary" href="{{ path('skill_edit', {"id": skill.id}) }}">Modifier les informations du profil</a>
		{{ include('skill/_delete_form.html.twig') }}
		<a href="{{ path('skill') }}">Retour à la liste</a>
	</div>
</div>{% endblock %}
