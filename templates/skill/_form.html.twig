{{ form_start(form) }}
	{{ form_row(form.name, {
            label: 'Nom de la compétence'
    }) }}
    {{ form_row(form.level, {
            label: 'Niveau de la compétence'
    }) }}
    {{ form_row(form.register, {
            label: 'Modifier'
    }) }}
{{ form_end(form) }}
