{{ form_start(form) }}
{% if is_granted("ROLE_SUPER_ADMIN") %}
	{{ form_row(form.user, {
                label: 'Nom de la personne'
        }) }}
{% endif %}
{{ form_row(form.nb_road, {
                label: 'Numéro de la rue'
        }) }}
{{ form_row(form.road, {
                label: 'Nom de la rue'
        }) }}
{{ form_row(form.city, {
                label: 'Nom de la ville'
        }) }}
{{ form_row(form.cp, {
                label: 'Code Postal'
        }) }}
{{ form_row(form.country, {
                label: 'Pays'
        }) }}
{{ form_row(form.register, {
                label: 'Modifier'
        }) }}
{{ form_end(form) }}
