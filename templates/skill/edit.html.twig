{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Modifier les informations</h1>

		{{ include('skill/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('skill') }}">Retour à la liste</a>

		{{ include('skill/_delete_form.html.twig') }}
	</div>
{% endblock %}