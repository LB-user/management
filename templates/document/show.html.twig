{% extends 'base.html.twig' %}

{% block body %}

	<div class="container">
		<h1>document</h1>
		<table class="table">
			<tbody>
				<tr>
					<th>Nom de la Personne</th>
					<td>{{ document.getUser().getLastname() }}
						{{ document.getUser().getFirstname() }}</td>
				</tr>
				<tr>
					<th>Nom du fichier</th>
					<td>{{ document.getName() }}</td>
				</tr>
			</tbody>
		</table>
		<a class="btn btn-primary" href="{{ path('document_edit', {"id": document.id}) }}">Modifier les informations du profil</a>
		{{ include('document/_delete_form.html.twig') }}
		<a href="{{ path('document') }}">Retour à la liste</a>
	</div>
</div>{% endblock %}
