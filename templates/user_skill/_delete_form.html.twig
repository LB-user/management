<form method="post" action="{{ path('user_skill_delete', {'id': user_skill.id}) }}" onsubmit="Êtes-Vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_skill.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
